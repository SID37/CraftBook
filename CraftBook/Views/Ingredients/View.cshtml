@model IEnumerable<CraftBook.Models.Ingredient>

<datalist id="ingredients">
    @foreach (var item in Model)
    {
        <option value="@item.Name" label="@item.Unit.ShortName" />
    }
</datalist>